{
  "name": "leetcode-custom-themes",
  "version": "1.0.0",
  "description": "Leetcode Custom Themes for Code Editor",
  "scripts": {
    "build": "npm run clean && copyfiles manifest.json ./*.js ./*.css ./*.html icons/** themes/** extension",
    "clean": "rm -rf extension",
    "deploy": "npm run build && bestzip extension.zip extension/"
  },
  "author": "Vishnuvardhan S",
  "license": "ISC",
  "dependencies": {
    "bestzip": "^2.2.0",
    "copyfiles": "^2.4.1"
  }
}
